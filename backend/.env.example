# 复制为 backend/.env 并填入真实值。`.env` 已被 .gitignore 忽略，`.env.example` 不会。
#
# 由 application.properties 的 spring.config.import 加载，文件不存在时应用照常启动
# （只是向量化不可用）。真实环境变量优先级更高，命令行临时覆盖依然有效：
#
#   EMBEDDING_API_KEY=xxx ./mvnw spring-boot:run
#
# 只放密钥。其余 Embedding 配置（base-url / model / dimensions / batch-size）都在
# application.properties 里有默认值，需要改再往这里加对应的环境变量名。
#
# 注意：本文件**不会**让 RealEmbeddingApiTests 跑起来。那个类用
# @EnabledIfEnvironmentVariable 判断真实环境变量，而这里提供的是 Spring 属性。
# 这是有意的——日常 ./mvnw verify 不该产生真实 API 费用。要跑它得显式传：
#
#   EMBEDDING_API_KEY=xxx ./mvnw test -Dtest=RealEmbeddingApiTests

EMBEDDING_API_KEY=
