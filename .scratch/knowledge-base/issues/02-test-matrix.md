# 02 — 测试：知识库模块测试用例矩阵

**What to build:** 完整的测试用例矩阵文档，**在后端实现开始之前完成**。先写清"什么算对"，实现才有明确目标；反过来先实现再补测试，测试往往只会验证已经写出来的行为，而不是应该有的行为。

矩阵要标出哪些用例可自动化、哪些必须手工验证，手工项本身也是正式的验收清单，不是"有空再做"。

**Blocked by:** None — can start immediately（技术方案三件套已就绪）

**Status:** ready-for-agent

- [ ] 产出 `tech/knowledge-base/test-matrix.md`，按功能域组织，每条用例含：前置状态 / 操作 / 预期结果 / 是否可自动化
- [ ] 覆盖 PRD §8 全部 7 条验收标准，每条能追溯到具体用例编号
- [ ] 正常场景、边界场景（空文档、单字符、恰好等于 `chunkSize`、`overlap` 边界、批量恰好 500 条）、异常场景（超限、格式不支持、并发冲突、Embedding 失败）三类齐全
- [ ] 逻辑删除一致性单列一组：每个查询方法都有对应用例，专门针对"漏写 `deleted = false`"这个高风险点
- [ ] 手工验证项明确列出，其中**上传 30MB 文件的堆内存峰值实测**标为最高优先级（它是唯一可能推翻技术方案的用例）
- [ ] 每个手工项写清具体的观察手段（用什么命令、看什么指标、什么数值算通过）
- [ ] `node scripts/check-doc-links.mjs` 通过
