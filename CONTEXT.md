# xrag 领域词汇表

本文件定义 xrag 项目在产品、设计、开发和测试中共同使用的业务语言。它只约束术语含义，不代表具体技术实现。

## 知识管理

### 知识库（Knowledge Base）

围绕同一业务范围组织的一组可检索资料。知识库是文档、权限和检索范围的主要归属边界；同一份资料是否允许属于多个知识库，需在业务规则中明确。

### 源文档（Source Document）

用户或外部系统交给 xrag 管理的一份原始资料，例如文件、网页或一段结构化内容。简称“文档”时默认指源文档，不指分块后的文本。

### 文档版本（Document Revision）

源文档在某一次内容变更后的不可混淆快照。新版本替代旧版本参与问答前，必须能区分它们的处理和索引状态。

### 入库任务（Ingestion Run）

把一个文档版本从原始内容转换为可检索内容的一次完整处理过程。一次入库任务可以成功、失败、取消或部分完成，并应能说明失败发生在哪一步。

### 文档分块（Document Chunk）

从某个文档版本中切出的最小检索内容单元。每个分块必须能追溯到原文位置，不能脱离源文档和文档版本独立存在。

### 分块策略（Chunking Strategy）

决定如何划分文档分块的一组可追溯规则，包括边界、大小、重叠和结构保留方式。同一文档版本采用不同策略产生的分块不能被视作同一批数据。

### 向量配置版本（Embedding Profile）

使文本向量可以相互比较的一组完整约定，包括模型及版本、输出维度、归一化方式和相似度口径。维度相同不代表向量配置版本相同；查询向量与被检索分块必须使用兼容的向量配置版本。

## 检索与问答

### 用户问题（Query）

用户在某一轮对话中提交、希望系统回答的原始表达。用户问题与系统为了检索生成的检索表达不是同一个概念。

### 检索表达（Retrieval Query）

系统根据用户问题、对话上下文和已识别意图生成的检索输入。一次用户问题可以产生多个检索表达。

### 意图（Intent）

系统对用户本轮目标的业务分类，用于决定检索哪些知识范围、是否调用工具以及采用何种回答策略。意图是可解释的路由依据，不等同于关键词。

### 检索通道（Retrieval Channel）

一次问答中面向特定知识范围或检索方式发起的独立召回路径。多个检索通道可以并行运行，其结果在生成答案前统一合并和排序。

### 召回结果（Retrieval Candidate）

某个检索通道返回、尚未确定进入最终上下文的候选文档分块。召回结果包含相关性信号和来源信息，不等同于最终引用。

### 回答（Answer）

系统针对一次用户问题给出的最终内容。回答可以是流式产生的，但业务上仍属于同一个可追踪结果。

### 引用（Citation）

回答中对具体文档版本和原文位置的可核验证据指向。仅记录知识库名称或相似文本不构成完整引用。

### 对话（Conversation）

同一用户目标下按顺序发生的多轮用户问题与回答。对话历史是生成上下文的一部分，但不能无界增长，也不能覆盖当前用户问题的含义。

## 工具与质量

### 工具（Tool）

系统可代表用户调用、用于取得外部信息或执行受控动作的能力定义。工具必须说明输入、输出、副作用和授权边界。

### 工具调用（Tool Call）

某次问答中对一个工具的具体执行记录。工具调用需要可追踪其参数、授权、结果、耗时和失败原因；有副作用的调用不能与只读查询等同处理。

### 评测用例（Evaluation Case）

用于重复衡量系统质量的一组固定输入和预期判断依据。评测用例可以检查检索、引用、回答或工具调用，但必须说明适用的知识和模型版本。

### 链路记录（Trace）

一次入库任务或问答经过各阶段时形成的关联记录。链路记录用于解释“做了什么、用了什么版本、在哪里失败”，不等同于普通应用日志。

## 关系速记

- 一个知识库包含源文档；一个源文档拥有一个或多个文档版本。
- 一个文档版本通过一次或多次入库任务产生文档分块。
- 文档分块按向量配置版本建立可检索表示。
- 一个用户问题属于一段对话，可生成多个检索表达并触发多个检索通道。
- 检索通道返回召回结果；最终回答可引用其中被采用的文档分块。
- 用户问题可以触发零个或多个工具调用；回答、引用和工具调用通过同一条链路记录关联。
