# 工单：AI 知识库建设模块

本目录是 [分角色任务书](../../docs/exec-plans/active/2026-07-25-knowledge-base-implementation-lanes.md) 的执行视角拆解：19 张垂直切片工单，按依赖顺序编号（阻塞者在前）。

每张工单是一条**跨层的完整可验证路径**，不是某一层的横向切片。例外是 `01`~`04`——它们是前置铺垫（"先让改动变容易，再做容易的改动"），本身不产生用户可见行为。

**推进方式**：做**前沿**（frontier）——任何"Blocked by 全部已完成"的工单都可以开始，不必严格按编号串行。

## 依赖图

```
01 ui-spec ──────────────┐
02 test-matrix           │
03 schema+config ──┬─────┼──> 05 知识库CRUD ──> 07 文件上传 ──┬──> 08 上传限流
                   │     │                        │           │
04 前端基础设施 ◀──┘     └────────────────────────┘           │
                   │                                          │
                   ├──> 06 EmbeddingClient ──┐                │
                   └──> 09 提取+分块 ────────┼──> 10 异步入库 ◀┘
                                             │        │
                                             └────────┼──> 11 卡死回收 ──┐
                                                      ├──> 12 文档管理 ──┼──> 18 逻辑删除专项测试 ──> 19 运维/CI
                                                      ├──> 13 分块查看编辑 ──> 14 分块新增/启禁 ──> 15 批量启禁 ──┘
                                                      └──> 16 URL来源 ──> 17 定时同步
```

## 状态速览

**20 张工单全部完成**，后端 **157 条测试**全绿，前端 `pnpm build` / `oxlint` 通过，`scripts/ci-validate.sh` exit 0。

`2026-07-26` 用户确认原型后，最后四处前端全部落地（工单 12 / 16 / 17 / 20）：

| 界面 | 此前的规格状态 | 结果 |
|---|---|---|
| 编辑文档 | 空白，原型挂 `notSpecced('编辑文档')` | [ui-spec §11](../../tech/knowledge-base/ui-spec.md) + 原型 + React 已落地 |
| 添加链接（URL 来源） | 空白，原型挂 `notSpecced('添加链接')` | [ui-spec §12](../../tech/knowledge-base/ui-spec.md) 同上 |
| 定时同步配置 | 状态展示 §5 有规定，配置表单没有 | 并入 §11 / §12 两个表单 |
| 查看源文档 | **PRD 里就没有这条需求** | PRD §4.2 增补 → [ui-spec §13](../../tech/knowledge-base/ui-spec.md) → 原型 → 实现 |

这四处都不是"忘了做"——前三处是原型用 `notSpecced()` 显式占位把规格缺口标了出来，第四处是用户看原型时发现的。**闸门起作用的样子就是这样：缺口在写代码之前就可见。**

**唯一未完成项**：工单 19 的"CI 在 GitHub 上实际跑绿一次"，需要推送才能验证。

模块核心路径已打通并可在浏览器演示：上传 / URL 抓取 → 触发 → 提取 → 切分 → 真实向量化 → 写库 → 界面查看、编辑、新增、单条与批量启禁用分块 → 文档启禁用与删除 → 定时同步按内容变化决定要不要重建。

| # | 工单 | Lane | 状态 |
|---|---|---|---|
| 01 | 产品：界面规格 + 交互原型 | 产品 | **done** `2026-07-25`（原型已确认） |
| 02 | 测试：测试用例矩阵 | 测试 | **done** `2026-07-25` |
| 03 | 数据库表结构与配置基线 | 后端 | **done** `2026-07-25` |
| 04 | 前端基础设施 | 前端 | **done** `2026-07-25` |
| 05 | 知识库增删改查全链路 | 后端+前端+测试 | **done** `2026-07-25` |
| 06 | Embedding 客户端接入 | 后端 | **done** `2026-07-25`（含真实 API 验证） |
| 07 | 本地文件上传（含内存实测） | 后端+前端+测试 | **done** `2026-07-25`（M-01 通过） |
| 08 | 上传并发限流 | 后端+测试 | **done** `2026-07-26` |
| 09 | 文本提取与分块算法 | 后端+测试 | **done** `2026-07-25` |
| 10 | 异步入库全链路 | 后端+前端+测试 | **done** `2026-07-25` |
| 11 | 卡死任务回收 | 后端+测试 | **done** `2026-07-26` |
| 12 | 文档配置更新/启禁用/删除 | 后端+前端+测试 | **done** `2026-07-26` |
| 13 | 分块查看与编辑 | 后端+前端+测试 | **done** `2026-07-25` |
| 14 | 分块新增与单条启禁用 | 后端+前端+测试 | **done** `2026-07-25` |
| 15 | 分块批量启禁用 | 后端+前端+测试 | **done** `2026-07-25` |
| 16 | URL 来源文档 | 后端+前端+测试 | **done** `2026-07-26` |
| 17 | 定时同步 | 后端+前端+测试 | **done** `2026-07-26` |
| 18 | 逻辑删除一致性专项测试 | 测试 | **done** `2026-07-26` |
| 19 | CI 集成测试与部署配置 | 运维 | **done** `2026-07-26`（CI 实跑待推送） |
| 20 | 查看源文档 | 后端+前端+测试 | **done** `2026-07-26` |

工单 **20 是 `2026-07-26` 新增的**，不在最初的 19 张里。起因是用户看原型时问"好像还缺少了查看源文档的功能"——查证后确认：**api.md 从来没有过访问原始文件的端点，PRD 里"原始文件"只出现在 §7.6 说的"删除时保留以便可恢复"**。不是漏做，是实现完成后回看界面才发现的能力缺口，而它最该出现的地方恰恰是分块页：手动干预要求用户判断"原文到底怎么写的"。
